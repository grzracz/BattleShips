#pragma once

#include <iostream>
#include <random>
#include <conio.h>
#include <string>
#include <Windows.h>
using std::cout;
using std::cin;
using std::endl;
using std::string;
using std::random_device;
using std::to_string;

void setConsoleColor(unsigned int color_number = 15);
